version: '3'

services:
  redis:
    image: 'redis'
    container_name: 'double_check_redis'
    ports:
      - "6379:6379"
    command: "redis-server --save 60 1 --loglevel warning"
    volumes:
      - './.redis_data:/data'
    
